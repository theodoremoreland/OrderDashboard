import { ReactElement } from "react";

import { LineChart } from "@mui/x-charts";

const Line = (): ReactElement => {
    return (
        <div>
            <h1>LineGraph</h1>
        </div>
    )
}

export default Line;